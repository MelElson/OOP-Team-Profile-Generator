const Employee  = require("../lib/Employee");

describe("User selects new employee, () => {
    const createEmployee = new Employee();
    expect(typeof(createEmployee)).toBe("object");
  });


  describe("User puts in id", () => {
    const createID = new ID();
    expect(typeof(createID)).toBe("object");
  });



















